<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.kt.mapper.GisMapper">

	<!-- 게시글 list -->
	<select id="selectList" resultType="com.kt.vo.GisVO">
	<!-- VO랑 변수명 맞춰야함 -->
	<![CDATA[
		SELECT
			  NAME
			 ,AGE
		 FROM GISTAR.TEST
	]]>
	</select>

	<!-- 선택된 구에 해당하는 상권 정보 가져오기  -->
	<select id="getSangList" parameterType="com.kt.vo.SangVO" resultType="com.kt.vo.SangVO">
	<!-- VO랑 변수명 맞춰야함 -->
	<![CDATA[
		 SELECT
			 sgg_cd
			,sgg_nm
			,substr(xcrd, 1, 9) xcrd
			,substr(ycrd, 1, 10) ycrd
		 FROM TRA_XYCRD
		 WHERE sgg_nm = #{sggnm}
		 LIMIT 1
	]]>
	</select>
	
	<!-- 서울시 구 정보 가져오기 -->
	<select id="getSeoulList" resultType="com.kt.vo.SangVO">
	<!-- VO랑 변수명 맞춰야함 -->
	<!-- VO는 언더바 빼야함-->
	<![CDATA[
		SELECT
			  SGG_CD
			  ,SGG_NM
		  FROM COMCD
	]]>
	</select>
	


</mapper>